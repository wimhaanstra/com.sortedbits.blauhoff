{
  "name": "cfe-battery-probe",
  "version": "0.1.0",
  "private": true,
  "description": "Research CLI to probe CFE / Smart Energy PLAT batteries on LAN and cloud",
  "type": "module",
  "scripts": {
    "probe-local": "tsx src/index.ts probe-local",
    "probe-cloud": "tsx src/index.ts probe-cloud"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "mqtt": "^5.10.4"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
